package mobi.eyeline.ips.web.components.input_text;

import com.sun.facelets.tag.jsf.ComponentConfig;

// TODO: Workaround for `jsfcomponents-38'. Remove once the issue is fixed.
public class InputTextHandler extends mobi.eyeline.util.jsf.components.input_text.InputTextHandler {

    public InputTextHandler(ComponentConfig config) {
        super(config);
    }

}
